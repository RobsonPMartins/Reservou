
// Hospedagem => estado / cidade / checkin / checkout

export interface Reservas {
    estado: string;
    cidade: string;
    checkin: string;
    checkout: string;
    hospedes: number;
    imagem: string;
}